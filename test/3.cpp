#include<stdio.h>

int main(void) {
  int a, b, c = 2, 5, 6;
  // 1. 배열 선언해야해 3차원
  // 2. 3차원 배열에 먼저 값 채워넣자
  int a[][][] = {{100, 100, 100}}
  
  // 3. 1면씩 돌고, 1행씩 돌고 1열씩 돌자 -> 3중 for문이네?
  for(int i=0; i<a; i++) {
    for (int j=0; j<b; j++) {
      for(int k=- ;k<c; k++) {
        // 4. 한 줄 돌때는 총점, 평균 구하고 넣으면서 가자
        // 5. 마지막줄 총점은 같은 열끼리 더하면서 가자

      }
    }
  }

  // 6. 총점의 평균은 예외적으로 ㅑ총합에 12나누자
  // 7. 처음부터 끝까지 배열 돌면서 출력하자
  for()forfor
  return 0;
}